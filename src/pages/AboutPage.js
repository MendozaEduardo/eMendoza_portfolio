import React from "react";
import {Content, Hero} from "../components";

export const AboutPage = (props) => {
  return (
    <div>
      <Hero title={props.title} />
      <Content>[Fill later]</Content>
    </div>
  );
};
